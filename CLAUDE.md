# Competition Integrity Rules

Follow every rule in `AGENTS.md`. In particular, modify only the two notebook cells
labeled `STUDENT WORKSPACE` and never bypass the challenge or compute the Mandelbrot
target analytically inside the submitted model.
